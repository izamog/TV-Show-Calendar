# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# claude code local settings
.claude/settings.local.json

# typescript
*.tsbuildinfo

# Added by `vercel link` — broader than the .env*.local rule above, so it also
# catches .env, .env.production, and the VERCEL_OIDC_TOKEN it writes locally.
.env*
# …but the committed template must stay visible to new contributors.
!.env.local.example
